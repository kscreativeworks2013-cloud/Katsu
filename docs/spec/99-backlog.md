# 未着手／次回以降の設計項目

完了済み：
・第3章｜UI/UX設計（全13画面の設計とアプリ本体への実装）
・第4章｜AIワークフロー＋データモデル（provenance、ステップ入出力契約、runStepの正式仕様、カスケード無効化、確認済み導線、確認待ちステータス、Run並走、差分プレビューの既定表示、ストリーミング判断、テストダブル方針、localStorage永続化）
・第5章｜画像アセットのデータモデルと Proposal Generator（確定版。origin、参照解決、テンプレートスロット、容量退避、章本文のフィールド化、Markdown 実出力、アセット登録UI）
・第6章｜Proposal IR と出力レンダラ（IR、revision、出所追跡、警告、Markdown／PDF／PowerPoint の実生成、日本語フォント埋め込み、外部URL画像の取り込み）

次の実装順：

1. **原寸アセットの扱い**。理由：埋め込み画像がサムネイル（最大96KB）である限り、現状の PDF は提出物として成立しない。「PDF が出る」ことと「PDF で提出できる」ことは別であり、後者が満たされるまでは実モデルを繋いでも実案件での通し検証にならない。localStorage に原寸は置けないため、**サーバー側永続化の一部（アセット実体の保存と取得）を前倒しで設計・実装する**。IR の image ブロックは、プレビュー用の縮小版と出力用の原寸を区別できる形に拡張する。
2. 実モデル接続（GenerationEngine の実装差し替え、設定画面のモデル別名との結線、画像生成によるAIアセット登録、失敗時の再試行とレート制御）
3. **ビジュアル主導の版面設計**。作業範囲は組版の微調整ではなく、画像を主役にした版面の設計。視覚表現の提案書である以上、1カラムの流し込みは提案内容そのものの説得力を損なう。表紙・ムードボード・ショットリストを中心に、画像が支配的な面付けと、テキストがそれに従属するレイアウトを定義する。PDF と PowerPoint の両方に適用し、ブランドカラーのテンプレート差し替えもここに含める。
4. API設計とサーバー側永続化の全体（localStorage からの移行、認証と権限。1で前倒しした部分を正式な形に収める）
5. Word レンダラ（第6章 6-5 のレンダラを1つ足すだけ。優先度は低い）

その後：
プロンプト設計書、エクスポート仕様の詳細、セキュリティ、テスト計画、デプロイ、運用手順。

第5章から持ち越した検討事項：
・日英 provenance の分離（優先度：中）。章本文は日英を同一フィールドに同居させているため、日本語だけを編集した章は英語版ごと保護され、再生成しても英語版が古いまま固定される経路がある。provenance を日英で分離するか、「英語版のみ再生成」の導線を設ける。海外提出を前提とするため放置しない。
・コレクション内のアイテム単位マージ（現状はコレクション全体で1フィールドとして保護する）
・AI生成アセットの登録（データモデルは第5章 5-1 で揃っている。実モデル接続時にエンジン側から登録する）

第4章から持ち越した検討事項：
・ストリーミング生成（現状は非ストリーミング。採用時の再設計見積もりは第4章 4-5 に記載）
・ステップ構成の見直し（8ステップは暫定。変更時はUIが設計に追従する）

第6章から持ち越した検討事項：
・上記1（原寸アセットの扱い）と3（ビジュアル主導の版面設計）。いずれも「提出できる成果物」に届くための前提。
・外部URL画像は登録時に取り込むが、保持するのは縮小版のみ。原寸は1で扱う。

現時点の実装スコープ外：
・実AIモデルの呼び出し（生成はテストダブルが担う。恒久的に残す）
・Word の実生成（レンダラ未実装。履歴のみ）
・画像アセットの原寸保存（現状は縮小版のみ）
・サーバー側の永続化とマルチユーザーの同時編集
